# Backend README

This backend is a minimal FastAPI skeleton for Sentinelcore. Use it as a starting point.

To run locally:

1. pip install -r backend/requirements.txt
2. uvicorn backend.app.main:app --reload --port 8000
